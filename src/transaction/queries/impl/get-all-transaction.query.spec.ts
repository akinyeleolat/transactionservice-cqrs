import { GetAllTransactionQuery } from './get-all-transaction.query';

describe('GetAllTransactionQuery', () => {
  it('should be defined', () => {
    expect(new GetAllTransactionQuery()).toBeDefined();
  });
});
